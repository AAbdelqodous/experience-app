package com.experience.kw.experience_app.controller;

import org.springframework.web.bind.annotation.RequestMapping;
import org.springframework.web.bind.annotation.RestController;

@RestController
@RequestMapping("/place-types")
public class PlaceTypeController {

    /*@PostMapping
    public ResponseEntity<PlaceType> addPlaceType(@RequestBody PlaceType placeType) {
        // Logic to add new place type
    }

    @GetMapping("/{placeTypeId}")
    public ResponseEntity<PlaceType> getPlaceType(@PathVariable Long placeTypeId) {
        // Logic to get place type by ID
    }

    @PutMapping("/{placeTypeId}")
    public ResponseEntity<PlaceType> updatePlaceType(@PathVariable Long placeTypeId, @RequestBody PlaceType placeType) {
        // Logic to update place type
    }

    @DeleteMapping("/{placeTypeId}")
    public ResponseEntity<Void> deletePlaceType(@PathVariable Long placeTypeId) {
        // Logic to delete place type
    }

    @GetMapping
    public ResponseEntity<List<PlaceType>> getAllPlaceTypes() {
        // Logic to get all place types
    }*/
}
