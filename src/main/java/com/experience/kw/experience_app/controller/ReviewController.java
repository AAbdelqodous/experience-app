package com.experience.kw.experience_app.controller;

import org.springframework.http.ResponseEntity;
import org.springframework.web.bind.annotation.*;

@RestController
@RequestMapping("/reviews")
public class ReviewController {

    /*@PostMapping
    public ResponseEntity<Review> addReview(@RequestBody Review review) {
        // Logic to add new review
    }

    @GetMapping("/{reviewId}")
    public ResponseEntity<Review> getReview(@PathVariable Long reviewId) {
        // Logic to get review by ID
    }

    @PutMapping("/{reviewId}")
    public ResponseEntity<Review> updateReview(@PathVariable Long reviewId, @RequestBody Review review) {
        // Logic to update review
    }

    @DeleteMapping("/{reviewId}")
    public ResponseEntity<Void> deleteReview(@PathVariable Long reviewId) {
        // Logic to delete review
    }

    @GetMapping("/professional/{professionalId}")
    public ResponseEntity<List<Review>> getReviewsForProfessional(@PathVariable Long professionalId) {
        // Logic to get reviews for a specific professional
    }

    @PostMapping("/{reviewId}/reply")
    public ResponseEntity<ReviewReply> addReply(@PathVariable Long reviewId, @RequestBody ReviewReply reply) {
        // Logic to add reply to a review
    }*/
}
