package com.experience.kw.experience_app.controller;

import org.springframework.web.bind.annotation.*;

@RestController
@RequestMapping("/user-experiences")
public class UserExperienceController {

    /*@PostMapping
    public ResponseEntity<UserExperience> addUserExperience(@RequestBody UserExperience userExperience) {
        // Logic to add new user experience
    }

    @GetMapping("/{userExperienceId}")
    public ResponseEntity<UserExperience> getUserExperience(@PathVariable Long userExperienceId) {
        // Logic to get user experience by ID
    }

    @PutMapping("/{userExperienceId}")
    public ResponseEntity<UserExperience> updateUserExperience(@PathVariable Long userExperienceId, @RequestBody UserExperience userExperience) {
        // Logic to update user experience
    }

    @DeleteMapping("/{userExperienceId}")
    public ResponseEntity<Void> deleteUserExperience(@PathVariable Long userExperienceId) {
        // Logic to delete user experience
    }

    @GetMapping("/user/{userId}")
    public ResponseEntity<List<UserExperience>> getUserExperiencesForUser(@PathVariable Long userId) {
        // Logic to get user experiences for a specific user
    }

    @GetMapping("/experience/{experienceId}")
    public ResponseEntity<List<UserExperience>> getUserExperiencesForExperience(@PathVariable Long experienceId) {
        // Logic to get user experiences for a specific experience
    }*/
}
